{
  "name": "f1-together",
  "private": true,
  "description": "F1 Together — Next.js app in f1-app/. Deploy with Firebase (firebase.json at repo root).",
  "scripts": {
    "dev": "cd f1-app && npm run dev",
    "build": "cd f1-app && npm run build",
    "start": "cd f1-app && npm run start",
    "lint": "cd f1-app && npm run lint",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  }
}
